/* General Page Styling */
body {
    font-family: Arial, sans-serif;
    text-align: center;
    background-color: #f8f9fa; /* Light background */
    margin: 0;
    padding: 0;
    position: relative;
}

/* Header */
header {
    background: gray;
    color: white;
    padding: 10px;
    font-size: 20px;
}

/* Container */
.container {
    background: white;
    padding: 20px;
    margin: 30px auto;
    width: 60%;
    box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
    border-radius: 10px;
    position: relative;
    z-index: 1;
}

/* Title Button */
.title-button {
    background: #1a2c6c;
    color: white;
    font-size: 16px;
    padding: 10px 20px;
    border: none;
    border-radius: 20px;
    cursor: pointer;
}

/* Text Areas */
.input-container {
    display: flex;
    justify-content: space-between;
    margin-top: 20px;
}

textarea {
    width: 48%;
    height: 150px;
    padding: 10px;
    border: 1px solid gray;
    border-radius: 5px;
    font-size: 14px;
    resize: none;
}

/* Upload Button */
.upload-container {
    margin-top: 15px;
    position: relative;
}

input[type="file"] {
    display: none;
}

.upload-label {
    display: inline-block;
    background: #f8f8f8;
    padding: 8px 15px;
    border: 1px solid #ddd;
    border-radius: 5px;
    cursor: pointer;
    font-size: 14px;
}

/* Summarize Button */
.summarize-btn {
    background: #1a2c6c;
    color: white;
    font-size: 16px;
    padding: 10px 20px;
    border: none;
    border-radius: 20px;
    cursor: pointer;
    margin-top: 10px;
}

/* ✅ Transparent Nepali Flag Watermark */
.bg-flag {
    position: fixed;
    bottom: 10px;
    left: 10px;
    width: 300px;
    height: auto;
    opacity: 0.1; /* Makes the flag semi-transparent */
    z-index: -1; /* Places it in the background */
}
